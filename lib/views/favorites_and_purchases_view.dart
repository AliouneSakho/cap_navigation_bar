import 'package:flutter/material.dart';

class FavoritesAndPurchasesView extends StatelessWidget {
  const FavoritesAndPurchasesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Favorites et purchases'),
      ),
    );
  }
}
